import React from 'react'

function Button({ text }) {
   return (
      <button>{text}</button>
   )
}

function Menu({ active=1 }) {
   <menu>
      LOGO.
      <section>
         
      </section>
   </menu>
}

export default Button